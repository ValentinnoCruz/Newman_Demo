version: 2.1

orbs:
  newman: postman/newman@1.0.0
  
jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - newman/newman-run:
          collection: https://www.getpostman.com/collections/57848d55309e2192ce86

workflows:
  example-workflow:
    jobs:
      - example-job